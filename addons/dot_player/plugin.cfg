[plugin]

name="dot-player"
description="Who is in the session and what represents them: an authoritative roster that holds a seat through a reconnect and mirrors to clients, a player node that is a container for components rather than a character, and the component base every other player addon extends. Depends only on dot-core."
author="dot"
version="0.1.0"
script="dot_player_plugin.gd"
